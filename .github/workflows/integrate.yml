---
name: Integrate

on:
  push:
    branches: [ "**" ]

jobs:
  enumerate:
    name: Container Enumeration
    runs-on: ubuntu-latest
    outputs:
      containers: ${{ steps.parse.outputs.containers }}
    steps:
      - name: Clone the repository
        uses: actions/checkout@v4
      - name: Parse compose.yml
        id: parse
        run: |
          containers=$(yq '[.services.[] | .build] | unique | map(select(. != null))' compose.yml -o json | jq -c .)
          echo "containers=$containers" >> $GITHUB_OUTPUT

  secrets:
    name: Secrets Check
    needs: enumerate
    runs-on: ubuntu-latest
    steps:
      - name: Clone the repository
        uses: actions/checkout@v4
      - name: Run Trufflehog
        id: trufflehog
        uses: trufflesecurity/trufflehog@main
        continue-on-error: false

  dockerfile:
    name: Dockerfile Lint Check
    needs: [ secrets, enumerate ]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        container: ${{ fromJson(needs.enumerate.outputs.containers) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v3
      - uses: hadolint/hadolint-action@v3.1.0
        with:
          failure-threshold: error
          dockerfile: ${{ matrix.container }}/Dockerfile
          
  qa:
    name: Quality Assurance Check
    needs: [ secrets, enumerate ]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        container: ${{ fromJson(needs.enumerate.outputs.containers) }}
      fail-fast: false
    steps:
      - name: Clone the repository
        uses: actions/checkout@v4
      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - name: Install Python dependencies
        run: python -m pip install black
      - name: Check code formatting
        run: black --diff --color --check ${{ matrix.container }}

  sca:
    name: Software Composition Analysis
    needs: [ secrets, enumerate ]
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: write
    strategy:
      matrix:
        container: ${{ fromJson(needs.enumerate.outputs.containers) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4
      - name: Generate an SBOM
        uses: anchore/sbom-action@v0
        with:
          path: ${{ matrix.container }}
          output-file: sbom-${{ matrix.container }}.json
          artifact-name: sbom-${{ matrix.container }}
      - name: Check for CVEs
        uses: anchore/scan-action@v3
        with:
          sbom: sbom-${{ matrix.container }}.json
          severity-cutoff: high

  sast:
    name: Static App Security Test
    needs: [ secrets, enumerate ]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        container: ${{ fromJson(needs.enumerate.outputs.containers) }}
      fail-fast: false
    container:
      image: semgrep/semgrep
    steps:
      - name: Clone the repository
        uses: actions/checkout@v4
      - name: Perform a SAST scan
        run: semgrep scan --config auto ${{ matrix.container }}
